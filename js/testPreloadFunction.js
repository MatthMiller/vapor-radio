// O objeto window possui os métodos e propriedades
// definidos no preload.js. Que podem executar funções
// do Windows
const { versions, testando } = window;
console.log(versions, testando);

// fazerBarulhinho executa uma função no processo main.js
// (Main Proccess). Que no caso é a função beep(), que
// é o barulho padrão do windows

// testando.fazerBarulhinho();
